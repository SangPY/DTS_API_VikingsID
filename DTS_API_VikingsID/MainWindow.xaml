<Window x:Class="DTS_API_VikingsID.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DTS_API_VikingsID"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="10">
            <TextBox x:Name="Username" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
            <PasswordBox x:Name="PasswordBox" Margin="0,10,0,0" />
            <TextBlock Text="{Binding StatusMessage}" Foreground="Red" Margin="0,10,0,0" />
            <Button Content="Login" Click="LoginButton_Click" Margin="0,10,0,0" />

            <TextBlock x:Name="txtResponse" Margin="10" Text="Kết quả trả về sẽ hiển thị ở đây" />

        </StackPanel>
    </Grid>
</Window>
